selenium==4.44.0
airium==0.2.7
packaging==26.2
PyYAML==6.0.3
googlemaps==4.10.0
Flask==3.1.3
Pillow==12.3.0
requests==2.34.2
Werkzeug==3.1.8
paho-mqtt==2.1.0
astral==3.2
tzdata==2026.3; sys_platform == "win32"
# Generic server core (config, registry, cache, Page, scheduling, MQTT relay).
# pip only honours #subdirectory= for VCS URLs, so this needs a git binary
# (the Dockerfile installs one). Pinned to a tag so a rebuild of this
# release gets the same epd. Bump it deliberately.
# For local development against a checkout instead:  pip install -e ../../epd/server
epd-server @ git+https://github.com/chrisjtwomey/epd.git@v0.2.0#subdirectory=server
